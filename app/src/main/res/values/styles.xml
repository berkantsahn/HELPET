<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>





    <style name="SwitchTheme" parent="Theme.AppCompat.Light">
        <!-- Aktif olduğu zaman ki renk -->
        <item name="colorControlActivated">#0000ff</item>

        <!-- Aktif olmadığı zaman ki renk -->
        <item name="colorSwitchThumbNormal">#00ffff</item>

        <!-- Aktif olmadığı zaman ki arkaplan rengi -->
        <item name="android:colorForeground">#505050</item>
    </style>



    <style name="switchcolors" parent="Theme.AppCompat.Light">
        <!-- Aktif olduğu zaman ki renk -->
        <item name="colorControlActivated">@color/tw_blue_dark</item>

        <!-- Aktif olmadığı zaman ki renk -->
        <item name="colorSwitchThumbNormal">@color/white</item>

        <!-- Aktif olmadığı zaman ki arkaplan rengi -->
        <item name="android:colorForeground">#505050</item>
    </style>


    <style name="TextLimitStyle" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="TextLimitError" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">#ff0000</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
